services:
  app:
    container_name: project1
    build:
      context: .
      dockerfile: Dockerfile
    volumes:
      - ./:/workspace
    working_dir: /workspace
    ports:
      - "8000:8000"
    tty: true
    stdin_open: true
